import * as vscode from 'vscode';

/**
 * Webview sidebar panel listing all CriticMarkup changes and comments.
 */

export class ChangesPanelProvider implements vscode.WebviewViewProvider {
    resolveWebviewView(
        webviewView: vscode.WebviewView
    ): void {
        // TODO: Implement
    }
}
