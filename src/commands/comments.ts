import * as vscode from 'vscode';

/**
 * Comment commands: add, edit, resolve, delete comments.
 * Uses CriticMarkup comment syntax {>> <<} and highlight syntax {== ==}.
 */

export function registerCommentCommands(context: vscode.ExtensionContext): void {
    // TODO: Register comment commands
}
