import * as vscode from 'vscode';

/**
 * Track changes recording: wraps edits in CriticMarkup syntax.
 * Handles insertions, deletions, and substitutions.
 */

export function registerTrackChangesCommands(context: vscode.ExtensionContext): void {
    // TODO: Register track changes commands
}
